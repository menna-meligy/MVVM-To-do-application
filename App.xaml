<common:BootStrapper
    x:Class="TodoXaml.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TodoXaml"
    xmlns:common="using:Template10.Common"
    RequestedTheme="Light">

    <Application.Resources>
        <Style TargetType="CommandBar" x:Key="HeaderCommandBarStyle">
            <Setter Property="DefaultLabelPosition" Value="Right"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Style="{ThemeResource TitleTextBlockStyle}" Margin="12,0,0,0"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</common:BootStrapper>
